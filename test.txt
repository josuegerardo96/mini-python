#"""
#	Código de prueba para
#	# Análisis Sintáctico #
#
#"""

"""
Hello
World
"""      #Esta si nos sirve (Borrar la de prueba para usar esta porque aunque esta comentado, sigue detectandolo el compilador)

#Calcular factorial
def calcularFac(num):
    #num_aux = 0
    if (num < 1):
    #num_aux = -1
        num_aux = 1
    else:
    #num_aux = num * (calcularFac(num--1))
        num_aux = num * (calcularFac(num-1))
    #return num_aux
return num_aux

#La asignacion fuera del if no nos esta funcionando y el return debe estar alineado al def para poder compilar

ventas = [100,200,300,400,500,-3.14]
# Funciona bien esta

Caracter = '%'
# Funciona bien esta


#promedio de elementos
def promedio(cualquier_arreglo):
    #tam = len(cualquier_arreglo)
    #resultado=0
    if(len(cualquier_arreglo)>0):
        tam = len(cualquier_arreglo)
    #ciclo para recorrer arreglo
    #cont = 0
    #sumatoria = 0
    #while (cont <= tam-1):
    #    sumatoria = sumatoria + cualquier_arreglo[cont]    No podemos declarar variables dentro de while
    #    cont = cont + 1                                    No podemos declarar variables dentro de while
    #if tam > 0:                                            tam no se declara antes, entonces no sirve
    #    resultado = sumatoria / tam
return resultado

tam = len(cualquier_arreglo)
while (cont <= tam-1):
    sumatoria = sumatoria + cualquier_arreglo[cont]             #Pero si nos sirve por fuera de la funcion


def calcularpromedioventas():
    #prom = promedio(len(ventas))
    print("El promedio de las ventas es: ")
    #print('\n')
    #print(prom)
# No podemos asignar valores a prom fuera de ifs y no se puede imprimir el caracter '\n'

print("Calculo del Factorial:%$ " + calcularFac(6))
#calcularpromedioventas()
#No podemos llamar a la funcion sola
